# voxflow-local-engine

Placeholder for the local AI engine defined in `ARCHITECTURE.md`.

Planned responsibilities:

- FastAPI WebSocket gateway on `ws://127.0.0.1:8765/ws`
- FunASR streaming ASR
- Local VAD, translation, and TTS providers
- Return ASR/translation/TTS events to the Chrome extension

No runtime implementation is included in this milestone.
